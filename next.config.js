/** @type {import('next').NextConfig} */
module.exports = {
  reactStrictMode: true,
  // Use this option if you want to deploy your static app to github pages
  // For example, if your repository name is 'mystatic' then the set this option to '/mystatic' 
  assetPrefix: ''
}
